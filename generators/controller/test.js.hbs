describe('{{#camelize}}{{name}}{{/camelize}}Ctrl', function() {
  var $scope;
  var {{#lowercamelize}}{{name}}{{/lowercamelize}}Ctrl;

  beforeEach(function() {
    module('app.controllers');
    inject(function($rootScope, $controller) {
      $scope = $rootScope.$new();
      {{#lowercamelize}}{{name}}{{/lowercamelize}}Ctrl = $controller('{{#camelize}}{{name}}{{/camelize}}Ctrl', {
        $scope: $scope
      });
    });
  });


});
