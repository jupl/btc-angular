describe('{{#lowercamelize}}{{name}}{{/lowercamelize}} filter', function() {
  var {{#lowercamelize}}{{name}}{{/lowercamelize}}Filter;

  beforeEach(function() {
    module('app.filters');
    inject(function($filter) {
      {{#lowercamelize}}{{name}}{{/lowercamelize}}Filter = $filter('{{#lowercamelize}}{{name}}{{/lowercamelize}}');
    });
  });


});
