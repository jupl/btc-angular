describe('{{#lowercamelize}}{{name}}{{/lowercamelize}} filter', function() {
  var {{#lowercamelize}}{{name}}{{/lowercamelize}};

  beforeEach(function() {
    module('app.filters');
    inject(function($filter) {
      {{#lowercamelize}}{{name}}{{/lowercamelize}} = $filter('{{#lowercamelize}}{{name}}{{/lowercamelize}}');
    });
  });

  it('', function() {

  });
});
