'use strict';

describe('{{humanize name}}', function() {
  var driver;

  before(function() {
    driver = getDriver();
  });

  beforeEach(function() {
    return driver.get(baseUrl);
  });

  it('', function() {

  });

  after(function() {
    driver.quit();
  });
});
