'use strict';

describe('{{humanize name}}', function() {
  var driver;

  before(function() {
    driver = getDriver();
  });

  it('', function() {
    return driver.get(baseUrl).then(function() {

    });
  });

  after(function() {
    driver.quit();
  });
});
