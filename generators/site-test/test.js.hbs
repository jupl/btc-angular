describe('{{#humanize}}{{name}}{{/humanize}}', function() {
  var driver;

  before(function() {
    driver = getDriver();
  });

  it('...', function() {
    return driver.get('http://localhost:3333');
  });

  after(function() {
    driver.quit();
  });
});
